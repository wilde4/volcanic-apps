.table-responsive
  %table.table
    %thead
      %tr
        %th #
        %th Name
        %th Referred Users
    %tbody
      -@referrals.each_with_index do |refdata, i|
        %tr
          %td= i+1
          %td= "#{refdata[0]}"
          %td= refdata[2]
      - if @referrals.empty?
        %tr
          %td{colspan: 4} No referrals made yet :(
