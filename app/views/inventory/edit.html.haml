%h4 Edit Inventory Item

= render 'form'

:javascript
  $(function() {
    $('form').submit(function() {
      var formData = $(this).serialize();
      $.ajax({
        url: "http://#{@app_server}/inventories/create_item.json",
        type: "POST",
        crossDomain: true,
        data: formData,
        dataType: 'json'
      }).success(function(data) {
        if(data.success) {
          window.location = "index";
        } else {
          $('.errors').html(data.status);
        }
        return false;
      });
      return false;
    });
  });

= link_to 'Back to Inventory', "index"